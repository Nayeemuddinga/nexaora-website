const canvas=document.getElementById("stars");const ctx=canvas?.getContext("2d");let w=0,h=0,stars=[];const reduced=window.matchMedia?.("(prefers-reduced-motion: reduce)").matches;
function resize(){if(!canvas||!ctx)return;w=canvas.width=innerWidth;h=canvas.height=innerHeight;const count=reduced?60:Math.min(180,Math.floor(w*h/9000));stars=Array.from({length:count},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*1.2+.15,a:Math.random()*.7+.1,s:Math.random()*.18+.03}))}
function draw(){if(!ctx)return;ctx.clearRect(0,0,w,h);for(const p of stars){p.y-=p.s;if(p.y<0)p.y=h;ctx.globalAlpha=p.a;ctx.fillStyle="#b8d8e8";ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}if(!reduced)requestAnimationFrame(draw)}
addEventListener("resize",resize,{passive:true});resize();draw();
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(el=>io.observe(el));
const menu=document.querySelector(".menu"),nav=document.querySelector("nav");menu?.addEventListener("click",()=>{const open=nav?.classList.toggle("mobile-open");menu.setAttribute("aria-expanded",String(!!open))});document.querySelectorAll("nav a").forEach(a=>a.addEventListener("click",()=>{nav?.classList.remove("mobile-open");menu?.setAttribute("aria-expanded","false")}));
addEventListener("keydown",e=>{if(e.key==="Escape"){nav?.classList.remove("mobile-open");menu?.setAttribute("aria-expanded","false");menu?.focus()}});
